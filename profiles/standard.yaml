# Standard application profile. Reference form — the loader currently reads standard.json
# (keep both in sync; see docs/PROFILES.md and DECISIONS.md ADR-0002).
name: standard
description: Standard application profile

applications:
  - id: Google.Chrome
    required: true
  - id: 7zip.7zip
    required: true
  - id: VideoLAN.VLC
    required: false
  - id: SumatraPDF.SumatraPDF
    required: false
